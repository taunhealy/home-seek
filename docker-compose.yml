version: '3.8'

services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./api:/app
      - ./.env:/app/.env
      - ./service-account.json:/app/service-account.json
    restart: always
